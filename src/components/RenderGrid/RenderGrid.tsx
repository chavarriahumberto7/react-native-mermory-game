/* eslint-disable prettier/prettier */
import { View, Text } from 'react-native';
import React from 'react';

export const RenderGrid = () => (
    <View>
        <Text>Render Grid</Text>
    </View>
);

export default RenderGrid;
